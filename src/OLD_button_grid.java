import java.awt.event.MouseAdapter;
import java.awt.event.MouseEvent;

public class OLD_button_grid {

	/*
	storage s;
	button_layered layer;
	int row;
	int column;
	
	public void start(storage store, int index_row, int index_column)
	{
		s = store;
		row = index_row; 
		column = index_column;
		s.debug.message("button_grid:start:row:" + row +":column:" + column);
		layer = new button_layered();
		//layer.start(s);
		//layer.button.setText(row + ":" + column);
		//layer.button.addMouseListener(new MouseAdapter() {
			@Override
			public void mousePressed(MouseEvent e)
			{
				if(e.getButton() == s.input.mouse_left_press) { // left click
					s.event.grid.grid_left_click(row, column);
				} else if(e.getButton() == s.input.mouse_right_press) { // right click
					s.event.grid.grid_right_click(row, column);
				} else if(e.getButton() ==s.input.mouse_scroll_press){ // scroll wheel pressed
					s.event.grid.grid_scroll_down(row, column);
				}
			}
		});
	}
	public void set_image(int type, int value)
	{
		layer.images[type] = value;
		layer.button.repaint();
	}
	public void repaint()
	{
		layer.button.repaint();
	}
	*/
}